Algoritmo "CalcularArea"

Var

base: real
altura: real
area: real

Inicio

escreval("Digite o valor da base do triângulo:")
leia(base)
escreval("Digite o valor da altura do triângulo:")
leia(altura)

area <- (base * altura)/2

escreval("A área do triângulo, com a base", base, " e a altura", altura, " é de:", area)

Fimalgoritmo